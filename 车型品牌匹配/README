read.cfg文件中配置训练的文件
[文件] [sheet表名] [车型](从0开始) [品牌](从0开始) [开始行数](从0开始)
例:data\一周的数据.xlsx 省心宝 2 4 1
test.cfg文件中配置测试的文件
[文件] [sheet表名] [sheet表索引](从0开始) [车型](从0开始) [品牌](从0开始) [开始行数](从0开始)
例:data\一周的数据-test.xlsx 省心宝 3 2 4 1

文件名因为分隔符是空格所以尽量不要存在空格
如果有特殊需要可以在源代码中进行修个有两个参数read_split，test_split,默认是已空格分隔的

程序主要采用的是dict，类似于map，同样的key，新的value会将老的覆盖掉，所以需要保证训练集的正确性
如demo的sheet为168的，在168中为宝马，但是在省心宝为MINI,省心宝的顺序较后，这里就会进行覆盖
为了跟多的匹配，将关键词中汉字保留，其余去除也同时作为一个key，提高了匹配到的概率
